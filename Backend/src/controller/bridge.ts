/*import { Options, PythonShell } from 'python-shell';

export default function runScript(keyword : string) {
    const options : Options = {
        mode : "text",
        pythonPath: 'C:\\Python310\\',
        pythonOptions: ['-u'], 
        scriptPath: 'D:\\Ders\\GitRepos\\Project-SANLP\\',
        args: [`${keyword}`]
    }

    PythonShell.run('main.py',options,(err,result) => {
        if(err){
            throw err;
        }
        else{
            console.log(result);
        }
        
    })
}
*/